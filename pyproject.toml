[project]
name = "bedtime-story-agent"
version = "0.1.0"
description = "A Flask bedtime story agent with LLM judging and revisions."
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "openai>=1.0.0",
]

[tool.uv]
package = false

